ul.traces
  - traces.each do |trace|
    li
      span= link_to trace.activity_id, trace_path(trace)
      span= link_to trace.origin_id || 'None', trace_path(trace)
      span= link_to trace.app_name, trace_path(trace)
      span= link_to trace.title, trace_path(trace)
      span= link_to trace.start, trace_path(trace)
      span= link_to "#{(trace.duration.to_f / 1_000_000).round(3)} ms", trace_path(trace)
