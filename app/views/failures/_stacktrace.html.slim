table.stacktrace
  - stacktrace.each do |l|
    tr
      td.file
        span= l['file'].split('/')[0..-2].join('/')
        span= '/' + l['file'].split('/')[-1].to_s
      td.line= l['line']
      td.call= l['call']

